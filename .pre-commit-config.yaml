repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.5.0
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json

  # check-dco: 验证 commit message 包含 Signed-off-by 行
  - repo: https://github.com/Ikalus1988/pre-commit-dco
    rev: 44b86d9
    hooks:
      - id: check-dco

  # 本地 hook: Lesson Schema + 质量门禁
  - repo: local
    hooks:
      - id: validate-lessons
        name: validate lesson schema
        entry: python3 scripts/validate_lessons.py
        language: system
        files: ^lessons/.*\.md$
        pass_filenames: false
      - id: check-lesson-quality
        name: check lesson quality (naming, content, forbidden patterns)
        entry: python3 scripts/check_lesson_quality.py
        language: system
        files: ^lessons/.*\.md$
        pass_filenames: false
      # gitleaks: 阻止密钥/Token 提交（本地二进制，无需网络）
      - id: gitleaks
        name: gitleaks secret scan
        entry: gitleaks git --pre-commit --redact --config .gitleaks.toml
        language: system
        always_run: true
        pass_filenames: false
